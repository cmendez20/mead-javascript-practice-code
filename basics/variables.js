// 1. You can't define a variable more than once. 
let petName = 'Rem';
petName = 'Spot'; // assigning a new value to a variable, not necessarily declaring a new variable.

console.log(petName);

// 2. There are rules related to variable names.
//  Can start a variable with a number. only $ & _ 

// 3. Variable names cannot be reserved keywords. 
// Cannot use let etc.,


